load() {
    local load1=$(cut -d' ' -f1 /proc/loadavg)
    local cpus=$(nproc)
    local percent=$(echo "scale=2; $load1 / $cpus * 100" | bc)

    echo "1-min load average: $load1"
    echo "Logical CPUs:       $cpus"
    echo "Saturation:         $percent%"
}
