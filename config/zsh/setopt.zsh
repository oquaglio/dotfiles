# cd to dir wthout cd
setopt AUTO_CD

# auto-correction for typos when changing directories or running commands
#setopt CORRECT

# Extends CORRECT to correct typos in command arguments as well, not just the command
# setopt CORRECT_ALL
