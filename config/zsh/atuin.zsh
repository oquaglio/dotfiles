# atuin — better shell history. Loaded after fzf so its Ctrl-R binding wins.
# Sourced via .sources (config/**/*.zsh pass), which runs after fzf.zsh in
# alphabetical order across the config tree.
eval_if_exists atuin init zsh
