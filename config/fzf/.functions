# Generic fzf-powered selectors (no specific domain).
#
#   fzcd            cd into a fuzzy-picked directory (alias: f)
#   fzrg <term>     ripgrep → fzf → $EDITOR at line
#
# Domain-specific fzf tools live with their domain:
#   functions/docker     dexec, dlog
#   functions/git        fbr, gco, fadd
#   functions/utils      fkill
#   config/aws/.functions    awsprofile
#   config/k8s/.functions    kpod

# Defensive: drop the old `alias fzcd=...` from previous shell sessions
# so this file's `fzcd()` function definition doesn't collide with it.
unalias fzcd 2>/dev/null

# Internal: locate the real fd / fdfind binary.
#
# `command -v fd` succeeds when `fd` is an *alias* (which it is on Ubuntu,
# where .aliases sets `alias fd='fdfind'`). The follow-up `command fd "$@"`
# then bypasses the alias and looks for a binary named `fd`, which doesn't
# exist — failing silently with no output. fzf then opens with zero entries.
# Filter to absolute paths so we only match real binaries.
_fzf_fd() {
    local bin
    bin=$(command -v fd 2>/dev/null)
    case "$bin" in /*) "$bin" "$@"; return ;; esac
    bin=$(command -v fdfind 2>/dev/null)
    case "$bin" in /*) "$bin" "$@"; return ;; esac
    return 127
}

# fzcd — fuzzy directory picker with eza tree preview, then cd.
fzcd() {
    local target
    target=$(_fzf_fd --type d --hidden --exclude .git . 2>/dev/null \
        | fzf --preview 'eza --tree --level=2 --color=always --icons {} 2>/dev/null || ls -la {}') \
        || return
    [ -n "$target" ] && cd -- "$target"
}

# fzrg — ripgrep + fzf with line-context preview, then open in $EDITOR.
fzrg() {
  local search_term="$1"
  if [ -z "$search_term" ]; then
    echo "Usage: fzrg <search_term>"
    return 1
  fi

  local rg_flags=(--color=always --line-number --no-heading)
    # Smart-case: case-insensitive when the term has no uppercase.
  [[ "$search_term" =~ [A-Z] ]] || rg_flags+=(--ignore-case)

  local selected
    selected=$(command rg "${rg_flags[@]}" "$search_term" 2>/dev/null \
        | fzf --ansi --delimiter=: \
              --preview 'l={2}; bat --style=numbers --color=always \
                  --highlight-line "$l" \
                  --line-range "$((l-5>0?l-5:1)):$((l+15))" {1} 2>/dev/null' \
              --preview-window='right:55%:wrap')

    [ -z "$selected" ] && return 0

    local file line
    file=$(awk -F: '{print $1; exit}' <<<"$selected")
    line=$(awk -F: '{print $2; exit}' <<<"$selected")
    if [[ "$line" =~ ^[0-9]+$ ]]; then
        "${EDITOR:-nvim}" +"$line" "$file"
    else
      echo "Could not extract a valid line number from: $selected"
    fi
}
